version: '2'

services:
  db:
    image: postgres:alpine
    ports:
      - '127.0.0.1:5432:5432'
    environment:
      - POSTGRES_USER=birdee
      - POSTGRES_PASSWORD=birdee
      - POSTGRES_DB=birdee
